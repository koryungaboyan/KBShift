<Window x:Class="KBShift.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KBShift.UI"
        mc:Ignorable="d"
        Title="KBShift Settings" Width="550"
        SizeToContent="Height"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Icon="Resources/keyboard.ico"
        Background="{DynamicResource WindowBg}" Foreground="{DynamicResource PrimaryText}">

    <Window.Resources>
        <!-- Global Text Style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        </Style>

        <!-- Default CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- ComboBox Item Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Background" Value="{DynamicResource PanelBg}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Expander Style to fix Header visibility -->
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Dynamic ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBg}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                           Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           Foreground="{TemplateBinding Foreground}"
                                           Grid.Column="2" Focusable="false"
                                           IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                           ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" CornerRadius="4" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Path Name="Arrow" Fill="{DynamicResource SecondaryText}" 
                                                  HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                  Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource BorderColor}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                               Content="{TemplateBinding SelectionBoxItem}"
                                               ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                               ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                               Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" Background="{DynamicResource PanelBg}" 
                                            BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="0,0,4,4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
        <StackPanel Margin="30">
            <Grid Margin="0,0,0,30">
                <TextBlock Text="Configuration" FontSize="28" FontWeight="Bold" HorizontalAlignment="Left" Foreground="{DynamicResource PrimaryText}"/>
                <Button Content="⌨ Show Keyboard" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Padding="10,5" Background="{DynamicResource ButtonBg}" Foreground="{DynamicResource AccentColor}" BorderBrush="{DynamicResource BorderColor}" 
                        Click="OskButton_Click" Cursor="Hand" ToolTip="Open On-Screen Keyboard">
                    <Button.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BorderColor}"/>
                                                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PanelBg}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <!-- Group 1 -->
            <TextBlock Text="Layout Group 1" Foreground="{DynamicResource AccentColor}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
            
            <TextBlock Text="Hotkey 1" Foreground="{DynamicResource SecondaryText}" FontSize="14" Margin="0,0,0,5"/>
            <ComboBox x:Name="Group1TriggerCombo" SelectionChanged="Group1TriggerCombo_SelectionChanged" 
                      Height="40" FontSize="14" Margin="0,0,0,20" Style="{StaticResource DarkComboBoxStyle}"/>

            <TextBlock Text="Layouts" Foreground="{DynamicResource SecondaryText}" FontSize="14" Margin="0,0,0,5"/>
            <ListBox x:Name="LeftLangCombo" SelectionMode="Multiple" 
                     SelectionChanged="LeftLangCombo_SelectionChanged"
                     Background="{DynamicResource PanelBg}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" FontSize="14">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}" 
                                  IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}"
                                  Margin="5,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Separator Background="{DynamicResource BorderColor}" Margin="0,30,0,30"/>

            <!-- Group 2 -->
            <TextBlock Text="Layout Group 2" Foreground="{DynamicResource AccentColor}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
            
            <TextBlock Text="Hotkey 2" Foreground="{DynamicResource SecondaryText}" FontSize="14" Margin="0,0,0,5"/>
            <ComboBox x:Name="Group2TriggerCombo" SelectionChanged="Group2TriggerCombo_SelectionChanged" 
                      Height="40" FontSize="14" Margin="0,0,0,20" Style="{StaticResource DarkComboBoxStyle}"/>

            <TextBlock Text="Layouts" Foreground="{DynamicResource SecondaryText}" FontSize="14" Margin="0,0,0,5"/>
            <ListBox x:Name="RightLangCombo" SelectionMode="Multiple" 
                     SelectionChanged="RightLangCombo_SelectionChanged"
                     Background="{DynamicResource PanelBg}" Foreground="{DynamicResource PrimaryText}" BorderBrush="{DynamicResource BorderColor}" FontSize="14">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                         <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}"
                                  IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}"
                                  Margin="5,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- About Section -->
            <Separator Background="{DynamicResource BorderColor}" Margin="0,40,0,20"/>
            <Expander Header="About" Foreground="{DynamicResource SecondaryText}" FontSize="14">
                <StackPanel Margin="10,10,0,0">
                    <TextBlock Text="KBShift v1.0.0" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Text="A lightweight utility to cycle keyboard languages using customizable modifiers." Foreground="{DynamicResource SecondaryText}" TextWrapping="Wrap" Margin="0,0,0,15"/>

                    <TextBlock Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,2">
                        Developed by: 
                        <Hyperlink NavigateUri="https://github.com/koryungaboyan" 
                                   RequestNavigate="Hyperlink_RequestNavigate">
                            Koryun Gaboyan
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Expander>
        </StackPanel>
    </Border>
</Window>