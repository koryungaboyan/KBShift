<Window x:Class="KBShift.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KBShift.UI"
        mc:Ignorable="d"
        Title="KBShift Settings" Width="550"
        SizeToContent="Height"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#121212" Foreground="#E0E0E0">

    <Window.Resources>
        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- ComboBox Item Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Custom Dark ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Grid.Column="2" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" 
                                          Focusable="false" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" CornerRadius="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="30" />
                                                </Grid.ColumnDefinitions>
                                                <Path Name="Arrow" Grid.Column="1" Fill="#888" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#2D2D2D" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,3,30,3" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True"  Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1E1E1E" BorderThickness="1" BorderBrush="#444"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#333333" BorderThickness="1">
        <StackPanel Margin="30">
            <Grid Margin="0,0,0,30">
                <TextBlock Text="Configuration" FontSize="28" FontWeight="Bold" HorizontalAlignment="Left" Foreground="#FFFFFF"/>
                <Button Content="⌨ OSK" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Padding="10,5" Background="#1E1E1E" Foreground="#BB86FC" BorderBrush="#333" 
                        Click="OskButton_Click" Cursor="Hand" ToolTip="Open On-Screen Keyboard">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <!-- Group 1 -->
            <TextBlock Text="Group 1" Foreground="#BB86FC" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
            
            <TextBlock Text="Trigger Trigger" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,5"/>
            <ComboBox x:Name="Group1TriggerCombo" SelectionChanged="Group1TriggerCombo_SelectionChanged" 
                      Height="40" FontSize="14" Margin="0,0,0,20" Style="{StaticResource DarkComboBoxStyle}"/>
            
            <TextBlock Text="Languages" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,5"/>
            <ListBox x:Name="LeftLangCombo" SelectionMode="Multiple" 
                     SelectionChanged="LeftLangCombo_SelectionChanged"
                     Background="#1E1E1E" Foreground="#E0E0E0" BorderBrush="#333" FontSize="14">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}" 
                                  IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}"
                                  Margin="5,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Separator Background="#333" Margin="0,30,0,30"/>

            <!-- Group 2 -->
            <TextBlock Text="Group 2" Foreground="#BB86FC" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
            
            <TextBlock Text="Trigger Trigger" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,5"/>
            <ComboBox x:Name="Group2TriggerCombo" SelectionChanged="Group2TriggerCombo_SelectionChanged" 
                      Height="40" FontSize="14" Margin="0,0,0,20" Style="{StaticResource DarkComboBoxStyle}"/>

            <TextBlock Text="Languages" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,5"/>
            <ListBox x:Name="RightLangCombo" SelectionMode="Multiple" 
                     SelectionChanged="RightLangCombo_SelectionChanged"
                     Background="#1E1E1E" Foreground="#E0E0E0" BorderBrush="#333" FontSize="14">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Background" Value="Transparent"/>
                         <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}"
                                  IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}"
                                  Margin="5,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- About Section -->
            <Separator Background="#333" Margin="0,40,0,20"/>
            <Expander Header="About KBShift" Foreground="#AAAAAA" FontSize="14">
                <StackPanel Margin="10,10,0,0">
                    <TextBlock Text="KBShift v1.0.0" Foreground="#FFFFFF" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Text="A lightweight utility to cycle keyboard languages using customizable modifiers." Foreground="#888" TextWrapping="Wrap" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="Developed by: Koryun Gaboyan" Foreground="#AAAAAA" Margin="0,0,0,2"/>
                </StackPanel>
            </Expander>
        </StackPanel>
    </Border>
</Window>